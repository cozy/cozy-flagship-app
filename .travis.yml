language: node_js

node_js:
  - 16

cache:
  yarn: true
  directories:
    - node_modules

stages:
 - lint
 - test

jobs:
  include:
    - name: Lint
      stage: lint
      script: yarn lint
    - name: Test
      stage: test
      script: yarn test --coverage
